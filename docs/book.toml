[book]
title = "Cowboy"
description = "An opinionated local coding agent that runs wild inside a host-native sandbox (Linux namespaces + Landlock, or macOS Seatbelt), while the host enforces security."
authors = ["cowboycode"]
language = "en"
src = "src"

[output.html]
default-theme = "navy"
preferred-dark-theme = "navy"
git-repository-url = "https://github.com/koshea/cowboy"
edit-url-template = "https://github.com/koshea/cowboy/edit/main/docs/{path}"

[output.html.fold]
enable = true
level = 1
